# see https://symfony.com/doc/current/reference/configuration/framework.html
framework:
    secret: '%env(APP_SECRET)%'

    # Pure JSON API with a stateless firewall: no sessions needed.
    session: false

    #esi: true
    #fragments: true

when@test:
    framework:
        test: true
